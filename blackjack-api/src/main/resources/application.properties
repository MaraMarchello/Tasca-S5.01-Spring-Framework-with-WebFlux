spring.application.name=blackjack-api

# MySQL Database Configuration (R2DBC for Player entities)
spring.r2dbc.url=r2dbc:mysql://localhost:3307/blackjack
spring.r2dbc.username=root
spring.r2dbc.password=78446660579

# Initialize schema
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# MongoDB Configuration (for Game entities)
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=blackjack
spring.data.mongodb.authentication-database=admin

# Logging
logging.level.org.springframework.data=DEBUG
logging.level.org.springframework.r2dbc=DEBUG
logging.level.com.blackjack=DEBUG

# Server Configuration
server.port=8080

# Swagger Configuration
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
